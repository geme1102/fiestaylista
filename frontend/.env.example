# === Frontend (obligatorio) ===
VITE_API_URL=http://localhost:3001
# Stripe eliminado — ahora usamos Mercado Pago (checkout redirect)
VITE_APP_URL=http://localhost:5173

# === Frontend (opcional) ===
VITE_SENTRY_DSN=
